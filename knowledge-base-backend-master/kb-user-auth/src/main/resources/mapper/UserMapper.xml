<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knowledge.base.userauth.mapper.UserMapper">

    <!-- Common query result mapping -->
    <resultMap id="BaseResultMap" type="com.knowledge.base.userauth.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="avatar" property="avatar" />
        <result column="real_name" property="realName" />
        <result column="department" property="department" />
        <result column="position" property="position" />
        <result column="remark" property="remark" />
        <result column="status" property="status" />
        <result column="last_login_time" property="lastLoginTime" />
        <result column="last_login_ip" property="lastLoginIp" />
        <result column="deleted" property="deleted" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="create_by" property="createBy" />
        <result column="update_by" property="updateBy" />
    </resultMap>

    <!-- Common query result columns -->
    <sql id="Base_Column_List">
        id, username, password, email, phone, avatar, real_name, department, position, remark, status,
        last_login_time, last_login_ip, deleted, created_at, updated_at, create_by, update_by
    </sql>

    <!-- Query a user by username -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM kb_user
        WHERE username = #{username}
        AND deleted = 0
        LIMIT 1
    </select>

    <!-- Query a user by email -->
    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM kb_user
        WHERE email = #{email}
        AND deleted = 0
        LIMIT 1
    </select>

    <!-- Query a user by phone number -->
    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM kb_user
        WHERE phone = #{phone}
        AND deleted = 0
        LIMIT 1
    </select>

    <!-- Count a user's documents (cross-database query against the kb_document view) -->
    <select id="countDocumentsByAuthorId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM kb_document WHERE author_id = #{authorId} AND deleted = 0
    </select>

    <!-- Sum a user's total likes received (cross-database query against the kb_document view) -->
    <select id="sumLikesByAuthorId" resultType="java.lang.Long">
        SELECT COALESCE(SUM(like_count), 0) FROM kb_document WHERE author_id = #{authorId} AND deleted = 0
    </select>

    <!-- Sum a user's total views received (cross-database query against the kb_document view) -->
    <select id="sumViewsByAuthorId" resultType="java.lang.Long">
        SELECT COALESCE(SUM(view_count), 0) FROM kb_document WHERE author_id = #{authorId} AND deleted = 0
    </select>

</mapper>
